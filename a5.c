#include<stdio.h>
int main()
{
int a;
 
printf("\n enter a :");
scanf("%d",&a);
if(a>0)
{
printf("\n\t a(%d) is a positive number",a);
}
else
{
printf("\n\t a(%d) is a negetive number",a);
}
return 0;
}
